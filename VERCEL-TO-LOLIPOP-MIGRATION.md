# Vercel → ロリポップ移行ガイド

## 📋 移行概要

**目的**: tarot.kanau-kiryu.com のホスティングを Vercel からロリポップに移行し、Vercel を解約

**重要**: この移行は旧アプリユーザーに影響を与えません（URLは変わらないため）

---

## ✅ 前提条件の確認

- ✅ ロリポップのアカウント: 有効
- ✅ ロリポップのサーバー: 空の状態
- ✅ tarot.kanau-kiryu.com: 現在Vercelを経由
- ✅ 新アプリ(v24): etarot.netlify.app を参照（Vercel不要）

---

## 📦 STEP 1: コンテンツの準備

### **1-1. Vercelから現在のコンテンツをダウンロード**

#### **方法A: Gitリポジトリから最新ビルド**
もしVercelにデプロイしているプロジェクトがGitで管理されている場合:

```powershell
# Vercelプロジェクトのディレクトリに移動
cd D:\project\tarot-old  # または該当のディレクトリ

# 最新のコードを取得（もしリポジトリがある場合）
git pull

# 静的ファイルをビルド（Next.jsの場合）
npm install
npm run build

# ビルドされたファイルは .next または out フォルダにあります
```

#### **方法B: Vercelから直接ダウンロード**
Vercelに既にデプロイされているファイルをそのまま使う場合:

```powershell
# Vercel CLIでデプロイ済みファイルを取得
# （Vercel CLIがインストールされている場合）
vercel pull --environment=production
```

#### **方法C: 手動でwgetを使用**
```powershell
# サイト全体をダウンロード（PowerShellの場合）
# wget がない場合は WSL や Git Bash で実行
wget --mirror --convert-links --adjust-extension --page-requisites --no-parent https://tarot.kanau-kiryu.com -P tarot-backup
```

---

## 📤 STEP 2: ロリポップにアップロード

### **2-1. FTP接続情報の確認**

ロリポップの管理画面で確認:
1. ログイン: https://user.lolipop.jp/
2. メニュー: **サーバーの管理・設定 > FTP・WebDAVアカウント**
3. 以下をメモ:
   - FTPサーバー: `ftp.lolipop.jp`
   - FTPアカウント: `your-account-la.jp`
   - FTPパスワード: `********`

### **2-2. アップロード先ディレクトリの確認**

ロリポップの管理画面で:
1. **独自ドメイン設定**を開く
2. `tarot.kanau-kiryu.com` のアップロードディレクトリを確認
   - 例: `/tarot/` または `/tarot.kanau-kiryu.com/`

### **2-3. ファイルをアップロード**

#### **オプションA: ロリポップFTPツール（ブラウザ）**
1. ロリポップ管理画面 > **サーバーの管理・設定 > ロリポップ！FTP**
2. アップロード先ディレクトリに移動（例: `/tarot/`）
3. Vercelからダウンロードしたファイルを**ZIPで圧縮**
4. ZIPファイルをアップロード
5. アップロード後、ZIP解凍機能で解凍

#### **オプションB: FTPクライアント（FileZilla等）**
1. FileZillaをダウンロード・インストール: https://filezilla-project.org/
2. 接続設定:
   - ホスト: `ftp.lolipop.jp`
   - ユーザー名: `your-account-la.jp`
   - パスワード: `********`
   - ポート: `21`
3. 接続後、アップロード先ディレクトリに移動
4. ローカルのファイルをすべてアップロード

#### **アップロードするファイル**
```
アップロード対象（Vercelからの静的ファイル）:
├─ index.html
├─ _next/
│  ├─ static/
│  └─ ...
├─ images/
├─ css/
├─ js/
└─ その他の静的ファイル
```

**重要**: `.next` フォルダではなく、`out` フォルダ（静的エクスポート後）の内容をアップロードしてください。

---

## 🌐 STEP 3: DNS設定の変更

### **3-1. ロリポップでドメイン設定**

1. ロリポップ管理画面 > **独自ドメイン設定**
2. すでに `tarot.kanau-kiryu.com` が設定されているか確認
3. 設定されていない場合:
   - **独自ドメイン設定**ボタン
   - ドメイン: `tarot.kanau-kiryu.com`
   - 公開フォルダ: `/tarot/`（またはアップロード先）
   - **独自ドメインをチェックする**

### **3-2. ムームードメイン（または使用中のDNSプロバイダ）でDNS変更**

#### **現在のDNS設定（Vercel向け）**
```
タイプ: CNAME または A
ホスト: tarot
値: cname.vercel-dns.com または Vercel IP
```

#### **新しいDNS設定（ロリポップ向け）**
ロリポップのIPアドレスを確認:
- ロリポップ管理画面 > **独自ドメイン設定** > IPアドレス表示
- 例: `157.7.190.xxx`

ムームードメイン（または使用中のDNSプロバイダ）で:
1. ログイン
2. `kanau-kiryu.com` のDNS設定を開く
3. `tarot` のCNAME/Aレコードを編集:
   ```
   タイプ: A
   ホスト: tarot
   値: 157.7.190.xxx（ロリポップのIPアドレス）
   TTL: 3600（1時間）
   ```
4. 保存

### **3-3. Vercelのドメイン設定を削除**

1. Vercel Dashboard: https://vercel.com/dashboard
2. 該当プロジェクトを開く
3. **Settings > Domains**
4. `tarot.kanau-kiryu.com` を削除:
   - **Remove** ボタン
   - 確認ダイアログで **Remove**

---

## ⏱️ STEP 4: DNS伝播を待つ

### **DNS伝播時間**
- **最短**: 15分〜1時間
- **通常**: 1〜6時間
- **最大**: 24〜48時間（TTL設定による）

### **伝播確認方法**

#### **オンラインツール**
https://www.whatsmydns.net/
- ドメイン入力: `tarot.kanau-kiryu.com`
- レコードタイプ: `A`
- 新しいIPアドレス（ロリポップ）が表示されればOK

#### **PowerShellコマンド**
```powershell
# DNSキャッシュをクリア
ipconfig /flushdns

# 現在のIPを確認
nslookup tarot.kanau-kiryu.com

# ロリポップのIPアドレスが返ってくればOK
# 例: Address: 157.7.190.xxx
```

#### **ブラウザで確認**
```powershell
# シークレットモードで開く
start msedge -inprivate https://tarot.kanau-kiryu.com

# または
start chrome --incognito https://tarot.kanau-kiryu.com
```

ページが正常に表示されればOK！

---

## ✅ STEP 5: 動作確認

### **5-1. 基本動作確認**

□ トップページが表示される
□ 画像が表示される
□ CSS/JSが正しく読み込まれる
□ 内部リンクが動作する
□ フォームが送信できる（もしあれば）
□ モバイルで表示確認

### **5-2. 旧アプリ（v23以前）で確認**

もし手元に旧バージョンのAPKがある場合:
1. テスト用Androidデバイスにインストール
2. アプリを起動
3. 正常に動作するか確認

**重要**: 旧アプリユーザーは `tarot.kanau-kiryu.com` を参照しているため、正常に動作する必要があります。

### **5-3. SSL証明書の確認**

ロリポップで無料SSL（Let's Encrypt）を有効化:
1. ロリポップ管理画面 > **セキュリティ > 独自SSL証明書導入**
2. `tarot.kanau-kiryu.com` の行で **SSL設定**
3. **無料独自SSLを設定する**
4. 設定完了まで5〜30分待つ
5. `https://tarot.kanau-kiryu.com` でアクセス確認

---

## 🗑️ STEP 6: Vercelプロジェクトの削除

### **⚠️ 削除前の最終確認**

□ `tarot.kanau-kiryu.com` がロリポップで正常動作
□ DNS伝播完了（24時間以上経過推奨）
□ 旧アプリで動作確認済み
□ Vercelのアクセスログがほぼゼロ（移行完了の証拠）

### **6-1. Vercelプロジェクトの削除**

1. Vercel Dashboard: https://vercel.com/dashboard
2. 該当プロジェクトを開く
3. **Settings** > **Advanced** > **Delete Project**
4. プロジェクト名を入力して確認
5. **Delete** ボタン

### **6-2. Vercelアカウントの解約（オプション）**

他にVercelプロジェクトがない場合:
1. Vercel Dashboard > **Account Settings**
2. **General** タブ
3. **Delete Account**
4. 確認手順に従って削除

---

## 📊 移行スケジュール（推奨）

| 日時 | 作業 | 所要時間 |
|------|------|---------|
| **Day 0** | コンテンツをロリポップにアップロード | 30分〜1時間 |
| **Day 0** | DNS設定変更 | 10分 |
| **Day 0〜1** | DNS伝播待ち | 1〜24時間 |
| **Day 1** | 動作確認（Web＋旧アプリ） | 30分 |
| **Day 1** | SSL証明書設定 | 5分（反映30分） |
| **Day 2〜7** | 監視期間（問題ないか確認） | - |
| **Day 7** | Vercelプロジェクト削除 | 5分 |

---

## 🚨 トラブルシューティング

### **問題: DNS変更後もVercelが表示される**

**原因**: ブラウザ・OSのDNSキャッシュ

**解決策**:
```powershell
# DNSキャッシュをクリア
ipconfig /flushdns

# ブラウザキャッシュもクリア（Ctrl+Shift+Delete）
# シークレットモードで再確認
```

### **問題: ロリポップで「ページが見つかりません」**

**原因**: ファイルのアップロード先が間違っている

**解決策**:
1. ロリポップFTPで正しいディレクトリを確認
2. `index.html` がルートにあるか確認
3. ディレクトリ設定を確認（独自ドメイン設定）

### **問題: CSSが効かない・画像が表示されない**

**原因**: 相対パスが正しくない

**解決策**:
1. ロリポップFTPでファイル構造を確認
2. `_next/static/` などのパスが正しいか確認
3. ブラウザのデベロッパーツール（F12）でエラー確認

### **問題: 旧アプリで動作しない**

**原因**: SSL証明書が未設定

**解決策**:
1. ロリポップで無料SSL証明書を設定
2. `https://tarot.kanau-kiryu.com` でアクセス確認
3. 旧アプリでリトライ

---

## ✅ 移行完了チェックリスト

### **技術的確認**
- □ ロリポップにすべてのファイルがアップロード済み
- □ DNS設定変更完了（Vercel → ロリポップIP）
- □ DNS伝播完了（24時間以上経過）
- □ SSL証明書設定完了（HTTPS動作）
- □ `tarot.kanau-kiryu.com` が正常に表示される

### **ユーザー影響確認**
- □ 旧アプリ（v23以前）で動作確認済み
- □ Webブラウザからのアクセスで正常表示
- □ モバイルでの表示確認済み
- □ 内部リンク・画像すべて動作

### **Vercel削除確認**
- □ Vercelのアクセスログがほぼゼロ（1週間）
- □ Vercelドメイン設定削除済み
- □ Vercelプロジェクト削除済み
- □ Vercelアカウント解約済み（オプション）

---

## 💰 コスト削減効果

### **Vercel料金（削減）**
- Hobby（無料プラン）: $0/月
- Pro プラン: $20/月
- **削減額**: $0〜$240/年

### **ロリポップ料金（既存）**
- スタンダードプラン: 約500円/月
- **変更なし**（既に契約中）

### **Netlify料金（新アプリ）**
- Starter（無料プラン）: $0/月
- **追加コストなし**

**結果**: Vercel解約で年間 $0〜$240 の削減 + 管理の簡素化 🎉

---

## 🎯 移行後の構成

```
┌─────────────────────────────────────────┐
│  新アプリ (v24+)                         │
│  └─ etarot.netlify.app                  │
│     ├─ Netlify CDN配信                  │
│     ├─ 自動デプロイ（GitHub連携）         │
│     └─ グローバル高速                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  旧アプリ (v23以前) - 徐々に減少         │
│  └─ tarot.kanau-kiryu.com               │
│     ├─ ロリポップで配信                  │
│     ├─ 既存契約活用                      │
│     └─ 安定稼働                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Vercel - 削除                          │
│  └─ 不要になったため解約 ✅              │
└─────────────────────────────────────────┘
```

---

## 📞 サポート連絡先

### **ロリポップ サポート**
- サポートページ: https://lolipop.jp/manual/
- お問い合わせ: https://lolipop.jp/contact/
- 電話サポート: 03-6822-4006（スタンダードプラン以上）

### **ムームードメイン サポート**
- サポートページ: https://muumuu-domain.com/about/support/
- お問い合わせ: https://muumuu-domain.com/support/contact/

---

## ✅ まとめ

### **この移行の目的**
- ✅ Vercelの解約でコスト削減・管理簡素化
- ✅ 旧アプリユーザーへのサービス継続（URL変更なし）
- ✅ 既存のロリポップ契約を有効活用

### **安全性**
- ✅ 旧アプリユーザーに影響なし（URL不変）
- ✅ 新アプリはNetlify参照（Vercel不要）
- ✅ DNS変更のみで移行完了

### **所要時間**
- 作業時間: **1〜2時間**
- DNS伝播: **1〜24時間**
- 監視期間: **1週間（推奨）**

---

**準備ができたら、STEP 1から順番に進めてください！** 🚀


